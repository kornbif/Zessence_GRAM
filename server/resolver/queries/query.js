const { category, categories } = require("./category");
const { service, services } = require("./service");

const Query = {
  categories,
  category,
  services,
  service
};
module.exports = Query;
